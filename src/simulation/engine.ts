// TODO: loop de simulação — percorre o calendário mestre (src/data/loaders/calendario.ts)
// e dispara `simularPartida`/`resolverChanceJogador` (match.ts) pras competições ativas
// de cada período, usando `obterRating`/`atualizarElo` (rating.ts) pra força dos times.
// Ainda não implementado: falta o estado de carreira (clube atual do jogador, calendário
// da temporada em andamento) que esse loop precisa consumir — ver src/career/career-loop.ts.
export {};
